{
  "name": "homebite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:client": "cd client && npm start",
    "start": "npm-run-all --parallel start:server start:client",
    "start:server": "cd server && npm start",
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "installall": "npm install && npm run install:client &&  npm run install:server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "react-icons": "^5.3.0",
    "sass": "^1.79.4"
  }
}
